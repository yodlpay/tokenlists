var name = "Default";
var logoURI = "https://raw.githubusercontent.com/cheq-pay/tokenlists/main/logos/tokens/0xB31f66AA3C1e785363F0875A1B74E27b85FD66c7/logo.png";
var keywords = [
];
var version = {
	major: 0,
	minor: 0,
	patch: 1
};
var tokens = [
	{
		chainId: 43113,
		name: "USDC",
		symbol: "USDC",
		logoURI: "https://raw.githubusercontent.com/cheq-pay/tokenlists/main/logos/testnet/usdc.svg",
		decimals: 18,
		tags: [
			"TESTNET"
		],
		address: "0x2601Df86814DcC0F3f98C7Ab97176ae19713503d"
	},
	{
		chainId: 43113,
		name: "USDT (Tether)",
		symbol: "USDT",
		logoURI: "https://raw.githubusercontent.com/cheq-pay/tokenlists/main/logos/testnet/usdt.svg",
		decimals: 18,
		tags: [
			"TESTNET"
		],
		address: "0x07D349103bC11105247Ec33D7deB1401206241e1"
	},
	{
		chainId: 43113,
		name: "DAI",
		symbol: "DAI",
		logoURI: "https://raw.githubusercontent.com/cheq-pay/tokenlists/main/logos/testnet/dai.svg",
		decimals: 18,
		tags: [
			"TESTNET"
		],
		address: "0x0f2A5D18aD37395266c0b59e3dfCFe790a0EE8E1"
	},
	{
		chainId: 43114,
		address: "0xd586E7F844cEa2F87f50152665BCbc2C279D8d70",
		decimals: 18,
		name: "Dai Stablecoin",
		symbol: "DAI.e",
		tags: [
			"Stablecoins"
		],
		logoURI: "https://raw.githubusercontent.com/cheq-pay/tokenlists/main/logos/tokens/0xd586E7F844cEa2F87f50152665BCbc2C279D8d70/logo.png"
	},
	{
		chainId: 43114,
		address: "0xc7198437980c041c805A1EDcbA50c1Ce5db95118",
		decimals: 6,
		name: "Tether Token - Bridged",
		symbol: "USDT.e",
		tags: [
			"Stablecoins"
		],
		logoURI: "https://raw.githubusercontent.com/cheq-pay/tokenlists/main/logos/tokens/0xc7198437980c041c805A1EDcbA50c1Ce5db95118/logo.png"
	},
	{
		chainId: 43114,
		address: "0xA7D7079b0FEaD91F3e65f86E8915Cb59c1a4C664",
		decimals: 6,
		name: "USD Coin - Bridged",
		symbol: "USDC.e",
		tags: [
			"Stablecoins"
		],
		logoURI: "https://raw.githubusercontent.com/cheq-pay/tokenlists/main/logos/tokens/0xA7D7079b0FEaD91F3e65f86E8915Cb59c1a4C664/logo.png"
	},
	{
		chainId: 43114,
		address: "0x130966628846BFd36ff31a822705796e8cb8C18D",
		decimals: 18,
		name: "Magic Internet Money",
		symbol: "MIM",
		tags: [
			"Stablecoins"
		],
		logoURI: "https://raw.githubusercontent.com/cheq-pay/tokenlists/main/logos/tokens/0x130966628846BFd36ff31a822705796e8cb8C18D/logo.png"
	},
	{
		chainId: 43114,
		address: "0xB97EF9Ef8734C71904D8002F8b6Bc66Dd9c48a6E",
		decimals: 6,
		name: "USD Coin",
		symbol: "USDC",
		tags: [
			"Stablecoins",
			"Top Tokens"
		],
		logoURI: "https://raw.githubusercontent.com/cheq-pay/tokenlists/main/logos/tokens/0xB97EF9Ef8734C71904D8002F8b6Bc66Dd9c48a6E/logo.png"
	},
	{
		chainId: 43114,
		address: "0x9702230A8Ea53601f5cD2dc00fDBc13d4dF4A8c7",
		decimals: 6,
		name: "Tether Token",
		symbol: "USDT",
		tags: [
			"Stablecoins",
			"Top Tokens"
		],
		logoURI: "https://raw.githubusercontent.com/cheq-pay/tokenlists/main/logos/tokens/0x9702230A8Ea53601f5cD2dc00fDBc13d4dF4A8c7/logo.png"
	},
	{
		chainId: 43114,
		address: "0xD24C2Ad096400B6FBcd2ad8B24E7acBc21A1da64",
		decimals: 18,
		name: "Frax",
		symbol: "FRAX",
		tags: [
			"Stablecoins"
		],
		logoURI: "https://raw.githubusercontent.com/cheq-pay/tokenlists/main/logos/tokens/0xD24C2Ad096400B6FBcd2ad8B24E7acBc21A1da64/logo.png"
	}
];
var timestamp = "2021-09-07T00:00:00+00:00";
var tokenlist = {
	name: name,
	logoURI: logoURI,
	keywords: keywords,
	version: version,
	tokens: tokens,
	timestamp: timestamp
};

var name$1 = "Default";
var logoURI$1 = "";
var keywords$1 = [
];
var version$1 = {
	major: 0,
	minor: 0,
	patch: 1
};
var chains = [
	{
		chainId: 5,
		logoURI: "https://raw.githubusercontent.com/cheq-pay/tokenlists/main/logos/chains/43113/logo.png",
		rpcUrls: [
			"https://rpc.ankr.com/eth_goerli"
		],
		explorerURL: "https://ropsten.etherscan.io",
		timestamp: "2021-09-07T00:00:00+00:00"
	},
	{
		chainId: 43113,
		logoURI: "https://raw.githubusercontent.com/cheq-pay/tokenlists/main/logos/chains/43113/logo.png",
		rpcUrls: [
			"https://api.avax-test.network/ext/bc/C/rpc"
		],
		explorerURL: "https://testnet.snowtrace.io/",
		timestamp: "2021-09-07T00:00:00+00:00"
	},
	{
		chainId: 4002,
		logoURI: "https://raw.githubusercontent.com/cheq-pay/tokenlists/main/logos/chains/43113/logo.png",
		rpcUrls: [
			"https://rpc.testnet.fantom.network/"
		],
		explorerURL: "https://testnet.ftmscan.com/",
		timestamp: "2021-09-07T00:00:00+00:00"
	},
	{
		chainId: 80001,
		logoURI: "https://raw.githubusercontent.com/cheq-pay/tokenlists/main/logos/chains/43113/logo.png",
		rpcUrls: [
			"https://rpc-mumbai.maticvigil.com/"
		],
		explorerURL: "https://mumbai.polygonscan.com/",
		timestamp: "2021-09-07T00:00:00+00:00"
	}
];
var chainlist = {
	name: name$1,
	logoURI: logoURI$1,
	keywords: keywords$1,
	version: version$1,
	chains: chains
};

var name$2 = "Default";
var logoURI$2 = "";
var keywords$2 = [
];
var version$2 = {
	major: 0,
	minor: 0,
	patch: 1
};
var routers = [
	{
		chainId: 43113,
		address: "0x439fe7388160589c8416dea6decb881bd8522a00",
		timestamp: "2021-09-07T00:00:00+00:00"
	},
	{
		chainId: 5,
		address: "0xad4a444c23776f8ed42b3d4bab836c7ba157270e",
		timestamp: "2021-09-07T00:00:00+00:00"
	},
	{
		chainId: 4002,
		address: "0x0aa77e0a32097ab660c83a6bc7bdbcf2323a27c1",
		timestamp: "2021-09-07T00:00:00+00:00"
	},
	{
		chainId: 80001,
		address: "0x900c649fa4030a796982bed361f52d9fbf62276b",
		timestamp: "2021-09-07T00:00:00+00:00"
	}
];
var routerlist = {
	name: name$2,
	logoURI: logoURI$2,
	keywords: keywords$2,
	version: version$2,
	routers: routers
};

var TokenlistTabNames;

(function (TokenlistTabNames) {
  TokenlistTabNames["PEG_USD"] = "USD pegged";
  TokenlistTabNames["PEG_EUR"] = "EUR pegged";
  TokenlistTabNames["STABLECOIN"] = "Stablecoin";
})(TokenlistTabNames || (TokenlistTabNames = {}));

/**
 * Comparator function that allows sorting version from lowest to highest
 * @param versionA version A to compare
 * @param versionB version B to compare
 * @returns -1 if versionA comes before versionB, 0 if versionA is equal to version B, and 1 if version A comes after version B
 */
function versionComparator(versionA, versionB) {
  if (versionA.major < versionB.major) {
    return -1;
  } else if (versionA.major > versionB.major) {
    return 1;
  } else if (versionA.minor < versionB.minor) {
    return -1;
  } else if (versionA.minor > versionB.minor) {
    return 1;
  } else if (versionA.patch < versionB.patch) {
    return -1;
  } else if (versionA.patch > versionB.patch) {
    return 1;
  } else {
    return 0;
  }
}

/**
 * Returns true if versionB is an update over versionA
 */

function isVersionUpdate(base, update) {
  return versionComparator(base, update) < 0;
}

/**
 * Enum describing types of version differences
 */
var VersionUpgrade;

(function (VersionUpgrade) {
  VersionUpgrade[VersionUpgrade["NONE"] = 0] = "NONE";
  VersionUpgrade[VersionUpgrade["PATCH"] = 1] = "PATCH";
  VersionUpgrade[VersionUpgrade["MINOR"] = 2] = "MINOR";
  VersionUpgrade[VersionUpgrade["MAJOR"] = 3] = "MAJOR";
})(VersionUpgrade || (VersionUpgrade = {}));
/**
 * Return the upgrade type from the base version to the update version.
 * Note that downgrades and equivalent versions are both treated as `NONE`.
 * @param base base list
 * @param update update to the list
 */


function getVersionUpgrade(base, update) {
  if (update.major > base.major) {
    return VersionUpgrade.MAJOR;
  }

  if (update.major < base.major) {
    return VersionUpgrade.NONE;
  }

  if (update.minor > base.minor) {
    return VersionUpgrade.MINOR;
  }

  if (update.minor < base.minor) {
    return VersionUpgrade.NONE;
  }

  return update.patch > base.patch ? VersionUpgrade.PATCH : VersionUpgrade.NONE;
}

/**
 * compares two token info key values
 * this subset of full deep equal functionality does not work on objects or object arrays
 * @param a comparison item a
 * @param b comparison item b
 */
function compareTokenInfoProperty(a, b) {
  if (a === b) return true;
  if (typeof a !== typeof b) return false;

  if (Array.isArray(a) && Array.isArray(b)) {
    return a.every(function (el, i) {
      return b[i] === el;
    });
  }

  return false;
}
/**
 * Computes the diff of a token list where the first argument is the base and the second argument is the updated list.
 * @param base base list
 * @param update updated list
 */


function diffTokenLists(base, update) {
  var indexedBase = base.reduce(function (memo, tokenInfo) {
    if (!memo[tokenInfo.chainId]) memo[tokenInfo.chainId] = {};
    memo[tokenInfo.chainId][tokenInfo.address] = tokenInfo;
    return memo;
  }, {});
  var newListUpdates = update.reduce(function (memo, tokenInfo) {
    var _indexedBase$tokenInf;

    var baseToken = (_indexedBase$tokenInf = indexedBase[tokenInfo.chainId]) == null ? void 0 : _indexedBase$tokenInf[tokenInfo.address];

    if (!baseToken) {
      memo.added.push(tokenInfo);
    } else {
      var changes = Object.keys(tokenInfo).filter(function (s) {
        return s !== "address" && s !== "chainId";
      }).filter(function (s) {
        return !compareTokenInfoProperty(tokenInfo[s], baseToken[s]);
      });

      if (changes.length > 0) {
        if (!memo.changed[tokenInfo.chainId]) {
          memo.changed[tokenInfo.chainId] = {};
        }

        memo.changed[tokenInfo.chainId][tokenInfo.address] = changes;
      }
    }

    if (!memo.index[tokenInfo.chainId]) {
      var _memo$index$tokenInfo;

      memo.index[tokenInfo.chainId] = (_memo$index$tokenInfo = {}, _memo$index$tokenInfo[tokenInfo.address] = true, _memo$index$tokenInfo);
    } else {
      memo.index[tokenInfo.chainId][tokenInfo.address] = true;
    }

    return memo;
  }, {
    added: [],
    changed: {},
    index: {}
  });
  var removed = base.reduce(function (list, curr) {
    if (!newListUpdates.index[curr.chainId] || !newListUpdates.index[curr.chainId][curr.address]) {
      list.push(curr);
    }

    return list;
  }, []);
  return {
    added: newListUpdates.added,
    changed: newListUpdates.changed,
    removed: removed
  };
}

/**
 * Returns the minimum version bump for the given list
 * @param baseList the base list of tokens
 * @param updatedList the updated list of tokens
 */

function minVersionBump(baseList, updatedList) {
  var diff = diffTokenLists(baseList, updatedList);
  if (diff.removed.length > 0) return VersionUpgrade.MAJOR;
  if (diff.added.length > 0) return VersionUpgrade.MINOR;
  if (Object.keys(diff.changed).length > 0) return VersionUpgrade.PATCH;
  return VersionUpgrade.NONE;
}

/**
 * Returns the next version of the list given a base version and the upgrade type
 * @param base current version
 * @param bump the upgrade type
 */

function nextVersion(base, bump) {
  switch (bump) {
    case VersionUpgrade.NONE:
      return base;

    case VersionUpgrade.MAJOR:
      return {
        major: base.major + 1,
        minor: 0,
        patch: 0
      };

    case VersionUpgrade.MINOR:
      return {
        major: base.major,
        minor: base.minor + 1,
        patch: 0
      };

    case VersionUpgrade.PATCH:
      return {
        major: base.major,
        minor: base.minor,
        patch: base.patch + 1
      };
  }
}

function getChain(chainId) {
  return chainlist.chains.find(function (needle) {
    return needle.chainId === chainId;
  });
}
function getTokens(chainId) {
  return tokenlist.tokens.filter(function (needle) {
    return needle.chainId === chainId;
  });
}
function getRouters(chainId) {
  return routerlist.routers.filter(function (needle) {
    return needle.chainId === chainId;
  });
}

export { TokenlistTabNames, VersionUpgrade, chainlist, diffTokenLists, getChain, getRouters, getTokens, getVersionUpgrade, isVersionUpdate, minVersionBump, nextVersion, routerlist, tokenlist, tokenlist as tokenschema, versionComparator };
//# sourceMappingURL=tokenlists.esm.js.map
